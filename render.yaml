services:
  - type: web
    name: araak-recruitment
    runtime: static
    branch: main
    buildCommand: echo "Araak Recruitment static site ready"
    staticPublishPath: .
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
